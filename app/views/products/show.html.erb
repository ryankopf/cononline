<%= link_to @product.vendor.name, @product.vendor %>

<p id="notice"><%= notice %></p>

<%= render @product, locals: {product: @product} %>

<%= link_to 'Edit', edit_product_path(@product) if false %>